# Dynamic Agent Correctness Benchmark 設計書

## 1. 目的

本設計は、AI エージェントが長いタスク遂行の途中で状況変化を受けても、正しく再計画し、一貫した成果物を出せるかを評価するためのベンチマークである。

特に評価したいのは単発の正答率ではなく、以下の能力である。

- 初期依頼を適切に構造化できるか
- 状態を正しく把握し続けられるか
- タスク依存関係を壊さずに実行できるか
- 前提変更時に再計画できるか
- 古い前提を捨てて新しい制約に従えるか
- 最終成果物が最新状態と整合しているか

この設計では、1 問を「静的な問題」ではなく「状態遷移を持つ連続タスク」として扱う。


## 2. ベンチマークの核となる考え方

典型例は以下である。

1. ユーザーが会議準備を依頼する
2. エージェントがタスク分解を行う
3. 数回の対話で要件が具体化される
4. エージェントが資料作成や日程調整を進める
5. 会議条件が途中で変わる
6. エージェントが影響範囲を特定し、計画を更新する
7. 成果物を修正して最終確認する

この一連の流れの中で、エージェントの正当性をターン単位で評価する。


## 3. タスク定義

各タスクは、以下の 5 要素で構成する。

- `Initial Request`
  初期依頼。曖昧さを含んでよい。
- `World State`
  現時点で有効な前提条件、制約、関係者、期限、予算、依存関係。
- `Allowed Actions`
  エージェントが取りうる行動の型。
- `Environment Events`
  途中で発生する前提変更や外乱。
- `Goal Condition`
  最終的に達成すべき状態。

本ベンチマークでは、エージェントは「正しい答えを一度出す」のではなく、「状況の変化に耐えながら正しく行動し続ける」必要がある。


## 4. 想定ドメイン

初期版では、構造が似ていて評価しやすい事務系タスクに絞る。

- 会議準備
- 出張・旅行手配
- 調査レポート作成
- 採用面接調整
- 小規模イベント運営

これらは共通して以下の特徴を持つ。

- 複数の依存タスクがある
- 人やリソースの制約がある
- 途中で条件変更が起こりやすい
- 成果物の整合性を判定しやすい


## 5. 1 ケースの基本構造

各ケースは、状態遷移シナリオとして定義する。

### 5.1 シナリオの流れ

1. 初期依頼を提示
2. エージェントが質問または計画提案
3. 環境が追加情報または変更イベントを返す
4. エージェントが実行・更新・確認を行う
5. 最終成果物を提出
6. 評価器がプロセスとアウトカムを採点

### 5.2 途中イベントの例

- 参加者が増えた
- 予算が減った
- 締切が前倒しになった
- 会場が使えなくなった
- 参照資料の最新版が差し替わった
- 優先順位が変わった
- キーパーソンが不在になった


## 6. 評価対象の能力軸

本ベンチマークでは、次の能力を分離して見る。

### 6.1 計画能力

- タスク分解が妥当か
- 順序付けが正しいか
- 依存関係を認識しているか

### 6.2 状態追跡能力

- どの前提が有効かを保持できているか
- 変更後の世界状態を内部表現に反映できているか

### 6.3 再計画能力

- 変更の影響範囲を正しく特定できるか
- 更新が必要なタスクのみを修正できるか
- 変更後の計画が最小限かつ整合的か

### 6.4 実行整合性

- 古い前提で作業を続けていないか
- 最新の制約を満たした成果物を出せているか

### 6.5 確認・保守性

- 重要な不確実性を確認できるか
- 実行前に必要な検証を入れられるか


## 7. 失敗モードの分類

このベンチマークの重要な点は、単純な成功率ではなく、何をどう間違えたかを記録することである。

初期版では以下の失敗ラベルを採用する。

- `state_staleness`
  古い前提や古い資料に基づいて動き続ける
- `missing_replan`
  前提変更が起きたのに再計画しない
- `partial_replan`
  一部しか更新せず、整合性が崩れる
- `invalid_dependency`
  タスクの依存順序を破壊する
- `goal_drift`
  元の依頼や更新後のゴールから逸脱する
- `unsafe_commit`
  確認不足のまま確定行動に進む
- `constraint_violation`
  予算、期限、人数制約などを破る
- `artifact_inconsistency`
  成果物内に古い前提と新しい前提が混在する


## 8. 評価指標

評価は 3 層構造にする。

### 8.1 Outcome Score

最終成果物が最新の世界状態と整合しているかを評価する。

例:

- 最新の参加者構成を反映しているか
- 最新の締切を守っているか
- 必須項目が欠けていないか
- 成果物同士で矛盾していないか

### 8.2 Process Score

各ターンでの行動が、その時点で合理的かを評価する。

例:

- 必要な確認を先に行っているか
- 依存関係に沿っているか
- 変更イベント発生後に古い計画を継続していないか

### 8.3 Recovery Score

前提変更後に、どれだけ正しく、どれだけ効率よく復帰できたかを評価する。

例:

- 何ターンで修正に入れたか
- 影響範囲の特定が正しいか
- 不要な手戻りを起こしていないか


## 9. スコアリング方針

総合スコアは以下で定義する。

```text
Total Score
= 0.40 * Outcome Score
+ 0.35 * Process Score
+ 0.25 * Recovery Score
```

補助指標として以下も保存する。

- 平均ターン数
- 不要アクション数
- 再計画までの遅延ターン数
- 失敗ラベル別件数
- ドメイン別スコア
- 難易度別スコア

初期版では 0-100 点で採点する。


## 10. 採点方式

採点器は `ルールベース + LLM 補助評価` の二段構成にする。

### 10.1 ルールベース採点

機械的に判定できるものを先に確定する。

- 締切違反
- 予算違反
- 依存順序違反
- 最新状態との不整合
- 必須項目の欠落
- 変更イベント未反映

### 10.2 LLM 補助採点

ルールだけでは評価しにくい部分を採点する。

- 再計画の質
- 質問の妥当性
- 実行方針の合理性
- 変更の影響範囲の説明の妥当性

### 10.3 採点の注意点

- LLM 採点は自由記述だけに頼らず rubric を固定する
- 同じケースを複数回採点し、ばらつきを観測する
- 可能な限りルール採点の比率を高める


## 11. データスキーマ

初期版では 1 ケースを JSON で保持する。

```json
{
  "task_id": "meeting_001",
  "domain": "meeting_prep",
  "difficulty": "medium",
  "initial_request": "来週の役員会議の準備をしてほしい",
  "initial_state": {
    "deadline": "2026-04-10T17:00:00+09:00",
    "participants": ["A", "B", "C"],
    "budget": 50000,
    "required_artifacts": ["agenda", "briefing_doc"],
    "constraints": [
      "参加者全員が参加可能な時間で設定する",
      "議題は3件以上必要"
    ]
  },
  "allowed_actions": [
    "ask_clarification",
    "propose_plan",
    "update_plan",
    "create_artifact",
    "revise_artifact",
    "finalize"
  ],
  "events": [
    {
      "turn": 3,
      "type": "state_change",
      "delta": {
        "participants_added": ["D"],
        "deadline": "2026-04-09T12:00:00+09:00"
      }
    }
  ],
  "goal_condition": {
    "must_satisfy_latest_state": true,
    "required_artifacts": ["agenda", "briefing_doc"],
    "no_constraint_violation": true
  },
  "rubric": {
    "outcome": [
      "成果物が最新参加者を反映している",
      "新締切に整合している"
    ],
    "process": [
      "変更後に旧計画を破棄した",
      "更新が必要な成果物を特定した"
    ],
    "recovery": [
      "2ターン以内に再計画した"
    ]
  }
}
```


## 12. 会話インターフェース仕様

エージェントと環境のやり取りは、再現性のために構造化する。

### 12.1 Agent 出力フォーマット

```json
{
  "action_type": "update_plan",
  "reasoning_summary": "参加者追加と締切前倒しのため、日程確認と資料更新を優先する",
  "state_assumptions": [
    "参加者Dを会議調整対象に追加する",
    "締切は2026-04-09 12:00 JSTに更新された"
  ],
  "plan_diff": {
    "remove": ["旧日程案の確定"],
    "add": ["Dの参加可能時間確認", "資料締切前倒し対応"]
  },
  "artifact_updates": [
    "agenda",
    "briefing_doc"
  ]
}
```

### 12.2 Environment 応答フォーマット

```json
{
  "event_type": "state_change",
  "message": "役員Dの参加が必須になりました。締切は明日正午です。",
  "delta": {
    "participants_added": ["D"],
    "deadline": "2026-04-09T12:00:00+09:00"
  }
}
```

構造化出力にすることで、採点器が `状態更新の認識有無` や `再計画の有無` を取りやすくなる。


## 13. 難易度設計

難易度は以下の因子で制御する。

- タスク数
- 依存関係の深さ
- 制約の数
- ステークホルダー数
- 変更イベント回数
- 変更イベントの破壊性
- 情報の不完全性
- 質問許容量

### 13.1 難易度レベル例

- `easy`
  変更 1 回、依存関係が浅い、制約が少ない
- `medium`
  変更 2 回、依存関係中程度、複数成果物あり
- `hard`
  変更 3 回以上、制約競合あり、部分観測あり


## 14. ベースラインエージェント

最低限、以下の 3 ベースラインを用意する。

### 14.1 No-Replan Baseline

初期計画のみを作り、その後の変更をほぼ無視する。

目的:

- 再計画能力の有無がスコアにどう効くかを可視化する

### 14.2 Reactive Replan Baseline

イベントが発生したときのみ計画更新する単純ベースライン。

目的:

- 最低限の再計画でどの程度通用するか測る

### 14.3 State-Aware Baseline

世界状態を内部表現として持ち、各ターンで差分更新しながら行動する。

目的:

- 本設計が狙う能力に対する強いベースラインを提供する


## 15. 推奨アーキテクチャ

実装は以下のモジュールに分ける。

### 15.1 `scenarios/`

ケース定義 JSON を置く。

### 15.2 `simulator/`

世界状態の更新、イベント適用、制約チェックを行う。

### 15.3 `agents/`

各エージェントの入出力アダプタを置く。

### 15.4 `evaluator/`

ルールベース採点、LLM 採点、失敗ラベル付与を行う。

### 15.5 `runner/`

ケース実行、ログ保存、集計、ランキング出力を行う。

想定ディレクトリ構成:

```text
AGI-Hackathon/
├─ README.md
├─ BENCHMARK_DESIGN_JA.md
├─ scenarios/
│  ├─ meeting/
│  ├─ travel/
│  └─ research/
├─ simulator/
├─ agents/
├─ evaluator/
├─ runner/
└─ results/
```


## 16. MVP の範囲

初回実装ではスコープを絞る。

### MVP でやること

- 3 ドメイン
- 各ドメイン 10 ケース
- easy / medium の 2 難易度
- ルール採点中心
- LLM 補助採点は一部項目のみ
- ベースライン 2 本

### MVP でやらないこと

- マルチエージェント協調
- 外部ツール実行の実環境評価
- 長文自由記述の完全自動採点
- hard 難易度の大規模ケース


## 17. 代表ケース例

### 17.1 会議準備ケース

初期依頼:

- 来週の役員会議の準備をしてほしい

初期制約:

- 参加者 A, B, C
- 議題 3 件以上
- 予算 5 万円
- アジェンダと事前説明資料が必須

途中イベント:

- D の参加が必須になる
- 締切が翌日正午に前倒し

正しい挙動:

- D を含む会議条件に更新
- 旧スケジュール案を破棄
- 資料更新を優先タスクに再配置
- 最終成果物に旧前提を残さない


## 18. ログ設計

再現性のため、以下を必ず保存する。

- ケース ID
- ターンごとの world state
- エージェント出力
- 適用イベント
- 採点結果
- 失敗ラベル
- 実行時のモデル名
- 実行日時

これにより、スコアだけでなく、どこで壊れたかを後から追跡できる。


## 19. コンペ向けの見せ方

この設計は単なる「業務自動化」ではなく、AGI 評価の文脈に接続できる。

主張ポイントは以下である。

- 静的ベンチマークでは測れない `動的整合性` を測る
- 正答率ではなく `変更後も正しい行動を維持できるか` を測る
- 失敗を型として捉え、能力差を分解して分析できる
- 実運用に近い `長期タスク + 状態変化` を扱う

特に ARC 系や agentic benchmark の流れを踏まえると、単発推論より `state tracking` と `replanning` を評価する設計は競争力がある。


## 20. 実装優先順位

以下の順で進めるのが現実的である。

1. ケース JSON スキーマを確定
2. 会議準備ドメインで 5 ケース作成
3. シンプルな simulator を実装
4. ルールベース evaluator を実装
5. No-Replan / Reactive Replan ベースラインを接続
6. スコア集計を追加
7. 他ドメインへ横展開
8. LLM 補助採点を追加


## 21. 直近の次アクション

この設計に基づいて、次に作るべきものは以下である。

- ケース JSON の正式スキーマ
- 会議準備ドメインのサンプルケース 5 本
- ルールベース評価器の判定仕様
- ベンチマーク実行用の最小 runner

この 4 点が揃えば、MVP として実際に回せる状態に入る。
