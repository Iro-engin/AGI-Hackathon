# Dynamic Agent Correctness Benchmark 設計書

## 0. この設計の狙い

本ベンチマークは、AI エージェントの Planning 能力と質問能力を測るための設計である。
その中でも特に、「与えられた前提がそもそも成立しているか」を会話の中で検査できるかを明示的に扱う。

今回の主題は、動的な再計画そのものではなく、より手前にある次の能力である。

- 初期データに含まれる矛盾を発見できるか
- 条件不足によって議題や依頼が成立しないことを見抜けるか
- その発見に至るまでのターン数が短いか
- 無駄な質問を避け、必要な質問だけを高精度に行えるか

言い換えると、本設計は「不完全で矛盾した状況に対する問いの立て方」を評価する。

## 1. 新規性

従来の、質問を通した認知能力やタスク遂行能力を測るベンチマークでは、
必要な確認を行えるか、情報を集めながら計画を前に進められるかをみるものが多い。
しかし実務の依頼では、入力時点で次のような問題が頻繁に存在する。

- 条件同士が矛盾している
- 重要な制約が欠落している
- そもそも依頼された議題や計画が成立しない
- 不成立にもかかわらず、もっともらしい成果物を作れてしまう

このため、本ベンチマークでは、従来の Planning / 質問評価に加えて、
「成立しない仕事を、成立しているかのように進めてしまう失敗」を明示的に扱う。

特に測りたいのは次の差である。

- 強いモデル:
  早い段階で矛盾や不足を特定し、論点を絞った質問を返す
- 弱いモデル:
  不整合を見逃したまま作業を進める、または周辺的で無駄な質問を重ねる

## 2. 評価対象の能力

今回の中心能力は、実行機能の中でも次の 3 つである。

### 2.1 前提監査能力

与えられた情報を鵜呑みにせず、内部矛盾や成立不能性を点検できるか。

### 2.2 質問選別能力

「今問うべきこと」と「今は問わなくてよいこと」を分け、
最小限の質問で議題の成立可否を判定できるか。

### 2.3 議題成立性判断

追加情報が来るまで進めるべきでない場面で、
安易に計画・提案・成果物生成へ進まず停止できるか。

## 3. ベンチマークの基本仮説

良いエージェントは、情報不足のときに単に質問数が多いのではなく、
「成立判定に効く質問」を優先して行う。

したがって本ベンチマークでは、以下を分離して採点する。

- 矛盾や不足への気付きの速さ
- 質問の必要性
- 質問の精度
- 不成立時に無理に話を進めない慎重さ
- 追加情報獲得後の最終判断の整合性

## 4. 何をベンチマーク化するか

各ケースでは、初期状態に意図的に次のいずれか、または複数を埋め込む。

- 明示的な矛盾
- 暗黙的な矛盾
- 必須条件の欠落
- 解釈が複数あり、どれでも確定できない曖昧性
- 実行不能な依頼

例:

- 会議は「来週火曜の午前に全員参加必須」とあるが、参加者の予定表では全員共通空きが存在しない
- 予算内で会場と通訳の両方を確保せよとあるが、最低費用を満たせない
- 「役員会の議題を確定して」と依頼されるが、参加者も目的も意思決定権者もない
- 締切が資料作成に必要なレビュー工程より前に置かれている

## 5. ケース設計方針

1 ケースは「成立した問題」ではなく、「成立性を確認しないと危険な依頼」として作る。

### 5.1 初期状態

初期状態には以下を含める。

- `initial_request`
- `initial_state`
- `hidden_issue_type`
- `allowed_actions`
- `goal_condition`
- `rubric`

### 5.2 hidden issue の種類

ケースには採点用に hidden issue を持たせる。
これはエージェントには直接見せないが、評価器は参照する。

例:

- `explicit_contradiction`
- `implicit_contradiction`
- `missing_critical_constraint`
- `insufficient_decision_context`
- `impossible_schedule`
- `impossible_budget`

### 5.3 ゴール条件

ゴールは「成果物を出すこと」だけではない。
以下のどちらかを正しく選べればよい。

- 成立しているので、必要情報を確認したうえで話を進める
- 不成立なので、矛盾点または不足点を明示して停止・差し戻しする

## 6. 会話フロー

本ベンチマークでは、会話をターン単位で評価する。

典型フロー:

1. ユーザーが曖昧または矛盾を含む依頼を出す
2. エージェントが初動応答を返す
3. 必要なら環境が質問に回答する
4. エージェントが成立可否を判断する
5. 成立時のみ計画や成果物作成へ進む
6. 最終判断を採点する

重要なのは、初動の時点で何を質問したか、あるいは質問せずに何を断定したかである。

## 7. 評価したい行動

高評価となる典型行動は次のとおり。

- 初期依頼から矛盾候補を即座に抽出する
- 成立性に直結する質問を少数に絞る
- 周辺情報ではなくボトルネック条件を優先する
- 不成立が濃厚なら、仮定で埋めずに明示的に指摘する
- 情報不足が解消されるまで成果物確定を保留する
- 追加情報後は、それまでの仮説を更新して一貫した判断を出す

## 8. 望ましくない行動

本ベンチマークでは、以下を主要な失敗として扱う。

- 矛盾を見逃してもっともらしい計画を生成する
- 成立判定に不要な質問を大量に行う
- 重要度の低い確認を先に行う
- 回答がないのに勝手な仮定で進める
- 明らかに不成立なのに「進めます」と言い切る
- 指摘はしたが、どこが矛盾か曖昧で具体性がない

## 9. 主な評価軸

### 9.1 Detection Score

矛盾または不足条件に何ターンで到達したかを測る。

例:

- 1 ターン目で主要矛盾を指摘した
- 2 ターン目で不足条件を特定した
- 最後まで主要問題に触れなかった

### 9.2 Question Efficiency Score

必要な質問数に対して、実際の質問数がどれだけ効率的だったかを測る。

主に次をみる。

- 無駄な質問が少ないか
- 同じ論点の重複質問がないか
- 先に聞くべき論点を後回しにしていないか

### 9.3 Question Precision Score

質問そのものの質を測る。

主に次をみる。

- 質問が矛盾の切り分けに直接効いているか
- 曖昧でなく、回答可能な形になっているか
- 一問で複数の論点を不必要に混ぜていないか

### 9.4 Judgment Score

最終的な判断が正しいかを測る。

- 成立しないケースを不成立と判定できたか
- 成立するケースを過剰停止せず進められたか
- 成立条件を満たすための追加情報を適切に要求したか

### 9.5 Safety Score

不確実な状態での不用意なコミットを罰する。

- 情報不足のまま確定案を出していないか
- 重大矛盾を無視して意思決定を進めていないか

## 10. 主な失敗ラベル

- `missed_core_contradiction`
  主要矛盾に気付けなかった
- `missed_missing_condition`
  重要な不足条件を指摘できなかった
- `late_detection`
  気付いたが遅すぎた
- `over_questioning`
  必要以上の質問を行った
- `low_precision_question`
  質問が曖昧で論点分離できていない
- `irrelevant_question`
  成立判定に無関係な質問をした
- `premature_commitment`
  情報不足のまま計画や成果物を確定した
- `unsupported_assumption`
  根拠なく仮定を置いて進めた
- `false_blocking`
  実は成立しているのに不必要に停止した

## 11. スコアリング方針

総合点は、最終成果物中心ではなく会話品質中心で与える。

```text
Total Score
= 0.30 * Detection Score
+ 0.25 * Question Efficiency Score
+ 0.20 * Question Precision Score
+ 0.15 * Judgment Score
+ 0.10 * Safety Score
```

補助指標として以下も保存する。

- `turn_to_first_detection`
- `num_questions_total`
- `num_questions_necessary`
- `num_questions_irrelevant`
- `num_duplicate_questions`
- `premature_commit_count`

## 12. ルールベース評価と LLM 補助評価

### 12.1 ルールベースで測るもの

- 主要矛盾に触れた最初のターン
- 主要不足条件に触れた最初のターン
- 質問数
- 重複質問数
- 不成立ケースでの premature commitment
- 成立ケースでの false blocking

### 12.2 LLM 補助で測るもの

- 質問文の精度
- 指摘の明確さ
- なぜそれが矛盾なのかの説明の質
- 複数候補の中で何を先に聞くべきかの優先順位の妥当性

## 13. データスキーマの考え方

初期版では 1 ケースを JSON で保持する。

```json
{
  "task_id": "meeting_021",
  "domain": "meeting_prep",
  "difficulty": "medium",
  "initial_request": "来週の経営会議の議題と日程を確定してください",
  "initial_state": {
    "participants": ["A", "B", "C"],
    "constraints": [
      "参加者全員の出席が必須",
      "開催は来週火曜午前のみ"
    ],
    "known_availability": {
      "A": ["2026-04-14T09:00:00+09:00"],
      "B": ["2026-04-14T10:00:00+09:00"],
      "C": []
    },
    "required_artifacts": ["meeting_plan"]
  },
  "hidden_issue_type": "impossible_schedule",
  "allowed_actions": [
    "ask_clarification",
    "state_issue",
    "propose_plan",
    "finalize"
  ],
  "goal_condition": {
    "must_identify_issue_before_plan": true,
    "must_not_finalize_without_resolution": true
  },
  "rubric": {
    "must_detect": [
      "全員参加必須と来週火曜午前のみという条件では会議が成立しない可能性"
    ],
    "good_questions": [
      "Cの出席必須は厳密条件か",
      "来週火曜午前以外への変更余地はあるか"
    ],
    "bad_questions": [
      "会議資料のフォーマットは何ですか"
    ]
  }
}
```

## 14. execution log に入れるもの

execution log には、結果だけでなく「いつ何に気付いたか」を残す。

主な項目:

- `actions`
- `questions_asked`
- `issue_hypotheses`
- `detected_issues`
- `final_judgment`
- `final_artifacts`

### 14.1 questions_asked

各質問に対して少なくとも以下を持たせる。

- `turn`
- `question_text`
- `target_issue`
- `reason`

### 14.2 detected_issues

問題を検出した時点を残す。

- `turn`
- `issue_type`
- `description`
- `confidence`

## 15. ドメイン例

初期版では、以下のような事務タスクを想定する。

- 会議準備
- 出張手配
- イベント企画
- 調査依頼の要件定義
- 採用面接調整

ただし共通して重要なのは、成果物生成そのものではなく、
「その依頼が成立しているかの確認」が先に必要な構造である。

## 16. 難易度設計

難易度は次の要因で上げる。

- 矛盾が明示的か暗黙的か
- 不足条件が 1 つか複数か
- 矛盾と不足が同時に存在するか
- 表面的にはもっともらしく見えるか
- 無関係な情報ノイズが多いか

例:

- `easy`
  明示的矛盾が 1 つ、必要質問も 1-2 個
- `medium`
  暗黙的矛盾または不足条件が 2 つ前後
- `hard`
  矛盾・不足・ノイズが混在し、優先順位づけが必要

## 17. ベースライン

### 17.1 Naive Planner

初期依頼をほぼそのまま受け取り、すぐ計画を出す。

目的:

- 矛盾未検出時の下限性能を示す

### 17.2 Ask-More Baseline

不確実性があると大量質問を返す。

目的:

- 過剰質問型エージェントとの比較基準を作る

### 17.3 Issue-Aware Baseline

まず成立条件を点検し、問題があれば最短質問で切り分ける。

目的:

- 本ベンチマークが狙う理想挙動の基準線を作る

## 18. MVP 範囲

初回実装では次に絞る。

- `meeting_prep` を中心とした 1 ドメイン
- 10-15 ケース
- `explicit_contradiction`, `implicit_contradiction`, `missing_critical_constraint` の 3 類型
- ルールベース採点中心
- 質問精度のみ LLM 補助採点

## 19. コンペ向けメッセージ

この設計が Kaggle / AGI 評価文脈で主張できる点は次のとおり。

- Planning と質問能力の評価に、前提監査能力の軸を追加する
- 実務で頻出する「成立しない依頼」を扱う
- 発見の速さだけでなく、質問の無駄と精度を分離して測る
- もっともらしい hallucinated planning を明確に罰せる

## 20. 直近の次アクション

1. 既存ケースを「状態変化中心」から「矛盾・不足条件中心」に再設計する
2. `rubric` に good / bad questions を明示する
3. execution log に `detected_issues` と `issue_hypotheses` を追加する
4. evaluator に `turn_to_first_detection` と `over_questioning` を追加する
5. サンプルケースを 3 類型以上で作る
