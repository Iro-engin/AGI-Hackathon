# Dynamic Agent Correctness Benchmark 設計書
## 0.Kaggleの課題と解く種類
従来指標では既存のデータやパターンに沿ったモデルの開発が主目的だった。しかし、今回はAGIとしてどれだけ近いか計るために、モデルの理解能力そのものに関する評価指標を作成したい。  
そのため、様々な認知能力(学習、メタ認知、注意、実行機能、社会認知)のうちどれか１つを計る指標を作成するのが今回のタスクである。  
- 学習 (Learning)：学習を通して新しい知識やスキルを習得し、適用できるか  
- メタ認知 (Metacognition)：自身が何を知っているか、何を知らないかを把握し、自己の思考プロセスを監視・制御できるか  
- 注意 (Attention)：目標に関連する情報に焦点を合わせ、無関係な情報を無視できるか  
- 実行機能 (Executive Functions)：計画立案、衝動抑制、認知的柔軟性を発揮できるか  
- 社会認知 (Social Cognition)：社会情報を処理・解釈し、社会的な状況において適切に反応する能力  

今回はこれら中でも、特に**実行機能**について解くことにした。  
## 1. リポジトリとKaggle 課題との関係

このリポジトリは、Kaggle の `Measuring AGI` で問われている、実行機能について評価するためのものである。  

Kaggle の文脈では、単に最終回答が合っているかだけでは不十分であり、
以下のような能力をどこまで測れるかが重要になる。

- 曖昧な依頼を構造化できるか(計画立案)
- タスクを依存関係つきで順序立てて進められるか(計画立案)
- 途中で変化する状態や制約を保持できるか(衝動抑制)
- 前提変更時に適切に再計画できるか(認知的柔軟性)
- 最終成果物が最新状態と整合しているか(認知的柔軟性)

本設計は、この中でも特に
`状態追跡 + 再計画 + 成果物整合性`
を明示的に評価できる形を目指している。


## 2. このリポジトリで取り組む対象

Kaggle 的な「AGI をどう測るか」という大きい問いに対して、
この初期版ではスコープをかなり絞っている。

具体的には、以下を評価対象にしている。

- 長めの事務タスクを、複数ターンの連続タスクとして扱うこと
- 途中で環境イベントが入り、前提が変わること
- 変化後にエージェントが質問、計画更新、成果物更新を行うこと
- 最終成果物だけでなく、途中プロセスも採点すること

つまりこの実装は、
「AGI 全体を一度に測る」のではなく、
`動的な業務タスクで破綻しやすい能力を先に切り出して測る`
ためのベンチマークである。


## 3. なぜこの方向で行くのか

この方向を選んだ理由は４つある。

### 3.1 失敗モードを明確に定義しやすい

会議準備や調整業務のようなタスクでは、
失敗が比較的具体的に観測できる。

- 古い締切のまま動く
- 参加者変更を成果物へ反映しない
- 依存関係を無視して先に進める
- 確認不足のまま確定する

このため、`state_staleness` や `missing_replan` のような
失敗ラベルを定義しやすく、改善ポイントも見えやすい。

### 3.2 ルールベース評価と相性が良い

Kaggle で扱う以上、採点の再現性は重要である。
会議準備のようなタスクでは、締切、参加者、成果物項目、依存順序など、
機械的に判定しやすい要素が多い。

そのため、自由記述の LLM judge に全面依存せず、
ルールベースでかなりの部分を安定して採点できる。

### 3.3 小さく始めて拡張しやすい

最初から多ドメイン・多能力を同時に扱うと、
ケース設計も採点器設計も散らばりやすい。

まずは `meeting_prep` のような
構造が明快で、状態変化を入れやすく、成果物も定義しやすいドメインから始めることで、

- JSON スキーマ
- execution log 形式
- failure label
- outcome / process / recovery の採点枠組み

を固めやすい。  
この枠組みが安定すれば、
出張手配、調査レポート、面接調整などへ水平展開しやすい。
### 3.4 評価用タスクがマイナーである

有名な試験問題や専門知識タスクを使うと、
モデルが事前学習で見た典型解答やテンプレートを再生して
高得点になる可能性がある。  
一方で、会議準備や面接調整のような事務タスクでは知識量そのものよりも、
「状態更新・依存関係管理・確認・再計画」といったマイナー
な実行機能の差が評価に出やすい。  
そのため、
「知っていたから解けた」のか
「状況変化に対応しながら正しく進められたのか」を
切り分けやすい。


## 4. 目的

本設計は、AI エージェントが長いタスク遂行の途中で状況変化を受けても、正しく再計画し、一貫した成果物を出せるかを評価するためのベンチマークである。

特に評価したいのは単発の正答率ではなく、以下の能力である。

- 初期依頼を適切に構造化できるか
- 状態を正しく把握し続けられるか
- タスク依存関係を壊さずに実行できるか
- 前提変更時に再計画できるか
- 古い前提を捨てて新しい制約に従えるか
- 最終成果物が最新状態と整合しているか

この設計では、1問を「静的な問題」ではなく「状態遷移を持つ連続タスク」として扱う。


## 5. ベンチマークの核となる考え方

典型例は以下である。

1. ユーザーが会議準備を依頼する
2. エージェントがタスク分解を行う
3. 数回の対話で要件が具体化される
4. エージェントが資料作成や日程調整を進める
5. 会議条件が途中で変わる
6. エージェントが影響範囲を特定し、計画を更新する
7. 成果物を修正して最終確認する

この一連の流れの中で、エージェントの正当性をターン単位で評価する。


## 6. タスク定義

各タスクは、以下の 5 要素で構成する。

- `Initial Request`
  初期依頼。曖昧さを含んでよい。
- `World State`
  現時点で有効な前提条件、制約、関係者、期限、予算、依存関係。
- `Allowed Actions`
  エージェントが取りうる行動の型。
- `Environment Events`
  途中で発生する前提変更や外乱。
- `Goal Condition`
  最終的に達成すべき状態。

本ベンチマークでは、エージェントは「正しい答えを一度出す」のではなく、「状況の変化に耐えながら正しく行動し続ける」必要がある。


## 7. 想定ドメイン

初期版では、構造が似ていて評価しやすい事務系タスクに絞る。

- 会議準備
- 出張・旅行手配
- 調査レポート作成
- 採用面接調整
- 小規模イベント運営

これらは共通して以下の特徴を持つ。

- 複数の依存タスクがある
- 人やリソースの制約がある
- 途中で条件変更が起こりやすい
- 成果物の整合性を判定しやすい


## 8. 1 ケースの基本構造

各ケースは、状態遷移シナリオとして定義する。

### 8.1 シナリオの流れ

1. 初期依頼を提示
2. エージェントが質問または計画提案
3. 環境が追加情報または変更イベントを返す
4. エージェントが実行・更新・確認を行う
5. 最終成果物を提出
6. 評価器がプロセスとアウトカムを採点

### 8.2 途中イベントの例

- 参加者が増えた
- 予算が減った
- 締切が前倒しになった
- 会場が使えなくなった
- 参照資料の最新版が差し替わった
- 優先順位が変わった
- キーパーソンが不在になった


## 9. 評価対象の能力軸

本ベンチマークでは、次の能力を分離して見る。

### 9.1 計画能力

- タスク分解が妥当か
- 順序付けが正しいか
- 依存関係を認識しているか

### 9.2 状態追跡能力

- どの前提が有効かを保持できているか
- 変更後の世界状態を内部表現に反映できているか

### 9.3 再計画能力

- 変更の影響範囲を正しく特定できるか
- 更新が必要なタスクのみを修正できるか
- 変更後の計画が最小限かつ整合的か

### 9.4 実行整合性

- 古い前提で作業を続けていないか
- 最新の制約を満たした成果物を出せているか

### 9.5 確認・保守性

- 重要な不確実性を確認できるか
- 実行前に必要な検証を入れられるか


## 10. 失敗モードの分類

このベンチマークの重要な点は、単純な成功率ではなく、何をどう間違えたかを記録することである。

初期版では以下の失敗ラベルを採用する。

- `state_staleness`
  古い前提や古い資料に基づいて動き続ける
- `missing_replan`
  前提変更が起きたのに再計画しない
- `partial_replan`
  一部しか更新せず、整合性が崩れる
- `invalid_dependency`
  タスクの依存順序を破壊する
- `goal_drift`
  元の依頼や更新後のゴールから逸脱する
- `unsafe_commit`
  確認不足のまま確定行動に進む
- `constraint_violation`
  予算、期限、人数制約などを破る
- `artifact_inconsistency`
  成果物内に古い前提と新しい前提が混在する


## 11. 評価指標

評価は 3 層構造にする。

### 11.1 Outcome Score

最終成果物が最新の世界状態と整合しているかを評価する。

例:

- 最新の参加者構成を反映しているか
- 最新の締切を守っているか
- 必須項目が欠けていないか
- 成果物同士で矛盾していないか

### 11.2 Process Score

各ターンでの行動が、その時点で合理的かを評価する。

例:

- 必要な確認を先に行っているか
- 依存関係に沿っているか
- 変更イベント発生後に古い計画を継続していないか

### 11.3 Recovery Score

前提変更後に、どれだけ正しく、どれだけ効率よく復帰できたかを評価する。

例:

- 何ターンで修正に入れたか
- 影響範囲の特定が正しいか
- 不要な手戻りを起こしていないか


## 12. スコアリング方針

総合スコアは以下で定義する。

```text
Total Score
= 0.40 * Outcome Score
+ 0.35 * Process Score
+ 0.25 * Recovery Score
```

補助指標として以下も保存する。

- 平均ターン数
- 不要アクション数
- 再計画までの遅延ターン数
- 失敗ラベル別件数
- ドメイン別スコア
- 難易度別スコア

初期版では 0-100 点で採点する。


## 13. 採点方式

採点器は `ルールベース + LLM 補助評価` の二段構成にする。

### 13.1 ルールベース採点

機械的に判定できるものを先に確定する。

- 締切違反
- 予算違反
- 依存順序違反
- 最新状態との不整合
- 必須項目の欠落
- 変更イベント未反映

### 13.2 LLM 補助採点

ルールだけでは評価しにくい部分を採点する。

- 再計画の質
- 質問の妥当性
- 実行方針の合理性
- 変更の影響範囲の説明の妥当性

### 13.3 採点の注意点

- LLM 採点は自由記述だけに頼らず rubric を固定する
- 同じケースを複数回採点し、ばらつきを観測する
- 可能な限りルール採点の比率を高める


## 14. データスキーマ

初期版では 1 ケースを JSON で保持する。

```json
{
  "task_id": "meeting_001",
  "domain": "meeting_prep",
  "difficulty": "medium",
  "initial_request": "来週の役員会議の準備をしてほしい",
  "initial_state": {
    "deadline": "2026-04-10T17:00:00+09:00",
    "participants": ["A", "B", "C"],
    "budget": 50000,
    "required_artifacts": ["agenda", "briefing_doc"],
    "constraints": [
      "参加者全員が参加可能な時間で設定する",
      "議題は3件以上必要"
    ]
  },
  "allowed_actions": [
    "ask_clarification",
    "propose_plan",
    "update_plan",
    "create_artifact",
    "revise_artifact",
    "finalize"
  ],
  "events": [
    {
      "turn": 3,
      "type": "state_change",
      "message": "役員Dの参加が必須になり、締切が翌日正午に前倒しになった",
      "delta": {
        "participants_added": ["D"],
        "deadline": "2026-04-09T12:00:00+09:00"
      }
    }
  ],
  "goal_condition": {
    "must_satisfy_latest_state": true,
    "required_artifacts": ["agenda", "briefing_doc"],
    "no_constraint_violation": true
  },
  "rubric": {
    "outcome": [
      "成果物が最新参加者を反映している",
      "新締切に整合している"
    ],
    "process": [
      "変更後に旧計画を破棄した",
      "更新が必要な成果物を特定した"
    ],
    "recovery": [
      "2ターン以内に再計画した"
    ]
  }
}
```


## 15. 会話インターフェース仕様

エージェントと環境のやり取りは、再現性のために構造化する。

### 15.1 Agent 出力フォーマット

```json
{
  "action_type": "update_plan",
  "reasoning_summary": "参加者追加と締切前倒しのため、日程確認と資料更新を優先する",
  "state_assumptions": [
    "参加者Dを会議調整対象に追加する",
    "締切は2026-04-09 12:00 JSTに更新された"
  ],
  "plan_diff": {
    "remove": ["旧日程案の確定"],
    "add": ["Dの参加可能時間確認", "資料締切前倒し対応"]
  },
  "artifact_updates": [
    "agenda",
    "briefing_doc"
  ]
}
```

### 15.2 Environment 応答フォーマット

```json
{
  "event_type": "state_change",
  "message": "役員Dの参加が必須になりました。締切は明日正午です。",
  "delta": {
    "participants_added": ["D"],
    "deadline": "2026-04-09T12:00:00+09:00"
  }
}
```

構造化出力にすることで、採点器が `状態更新の認識有無` や `再計画の有無` を取りやすくなる。


## 16. 難易度設計

難易度は以下の因子で制御する。

- タスク数
- 依存関係の深さ
- 制約の数
- ステークホルダー数
- 変更イベント回数
- 変更イベントの破壊性
- 情報の不完全性
- 質問許容量

### 16.1 難易度レベル例

- `easy`
  変更 1 回、依存関係が浅い、制約が少ない
- `medium`
  変更 2 回、依存関係中程度、複数成果物あり
- `hard`
  変更 3 回以上、制約競合あり、部分観測あり


## 17. ベースラインエージェント

最低限、以下の 3 ベースラインを用意する。

### 17.1 No-Replan Baseline

初期計画のみを作り、その後の変更をほぼ無視する。

目的:

- 再計画能力の有無がスコアにどう効くかを可視化する

### 17.2 Reactive Replan Baseline

イベントが発生したときのみ計画更新する単純ベースライン。

目的:

- 最低限の再計画でどの程度通用するか測る

### 17.3 State-Aware Baseline

世界状態を内部表現として持ち、各ターンで差分更新しながら行動する。

目的:

- 本設計が狙う能力に対する強いベースラインを提供する


## 18. 推奨アーキテクチャ

実装は以下のモジュールに分ける。

### 18.1 `scenarios/`

ケース定義 JSON を置く。

### 18.2 `simulator/`

世界状態の更新、イベント適用、制約チェックを行う。

### 18.3 `agents/`

各エージェントの入出力アダプタを置く。

### 18.4 `evaluator/`

ルールベース採点、LLM 採点、失敗ラベル付与を行う。

### 18.5 `runner/`

ケース実行、ログ保存、集計、ランキング出力を行う。

想定ディレクトリ構成:

```text
AGI-Hackathon/
├─ README.md
├─ BENCHMARK_DESIGN_JA.md
├─ scenarios/
│  ├─ meeting/
│  ├─ travel/
│  └─ research/
├─ simulator/
├─ agents/
├─ evaluator/
├─ runner/
└─ results/
```


## 19. MVP の範囲

初回実装ではスコープを絞る。

### MVP でやること

- 3 ドメイン
- 各ドメイン 10 ケース
- easy / medium の 2 難易度
- ルール採点中心
- LLM 補助採点は一部項目のみ
- ベースライン 2 本

### MVP でやらないこと

- マルチエージェント協調
- 外部ツール実行の実環境評価
- 長文自由記述の完全自動採点
- hard 難易度の大規模ケース


## 20. 代表ケース例

### 20.1 会議準備ケース

初期依頼:

- 来週の役員会議の準備をしてほしい

初期制約:

- 参加者 A, B, C
- 議題 3 件以上
- 予算 5 万円
- アジェンダと事前説明資料が必須

途中イベント:

- D の参加が必須になる
- 締切が翌日正午に前倒し

正しい挙動:

- D を含む会議条件に更新
- 旧スケジュール案を破棄
- 資料更新を優先タスクに再配置
- 最終成果物に旧前提を残さない


## 21. ログ設計

再現性のため、以下を必ず保存する。

- ケース ID
- ターンごとの world state
- エージェント出力
- 適用イベント
- 採点結果
- 失敗ラベル
- 実行時のモデル名
- 実行日時

これにより、スコアだけでなく、どこで壊れたかを後から追跡できる。


## 22. コンペ向けの見せ方

この設計は単なる「業務自動化」ではなく、AGI 評価の文脈に接続できる。

主張ポイントは以下である。

- 静的ベンチマークでは測れない `動的整合性` を測る
- 正答率ではなく `変更後も正しい行動を維持できるか` を測る
- 失敗を型として捉え、能力差を分解して分析できる
- 実運用に近い `長期タスク + 状態変化` を扱う

特に ARC 系や agentic benchmark の流れを踏まえると、単発推論より `state tracking` と `replanning` を評価する設計は競争力がある。


## 23. 実装優先順位

以下の順で進めるのが現実的である。

1. ケース JSON スキーマを確定
2. 会議準備ドメインで 5 ケース作成
3. シンプルな simulator を実装
4. ルールベース evaluator を実装
5. No-Replan / Reactive Replan ベースラインを接続
6. スコア集計を追加
7. 他ドメインへ横展開
8. LLM 補助採点を追加


## 24. 直近の次アクション

この設計に基づいて、次に作るべきものは以下である。

- ケース JSON の正式スキーマ
- 会議準備ドメインのサンプルケース 5 本
- ルールベース評価器の判定仕様
- ベンチマーク実行用の最小 runner

この 4 点が揃えば、MVP として実際に回せる状態に入る。
